#ifndef TIMAGEHANDLER_H
#define TIMAGEHANDLER_H

#include "Basic.h"
/*
#include <png.h>

class TImageHandler
	{
	public:
		TImageHandler();
		~TImageHandler();
		bool LoadPNG( char* aFileName );
		bool SavePNG( char* aFileName );
		bool CreatePNG( int aWidth, int aHeight);
		void ModifyPNG();
		//bool LoadBMP( CMesh* aMesh, char* aFileName );
	private:
		int iX, iY;
		int iWidth, iHeight;

		png_byte iColorType;
		png_byte iBitDepth;
		
		png_structp iPngData;
		png_infop	iPngInfo;
		png_infop	iPngEndInfo;

		int iNumberOfPasses;
		png_bytepp iPngRows;
		//unsigned char* iPngRows;

	};
//CALLBACKS
//extern void readChunkCallback( png_ptr ptr, png_unknown_chunkp chunk );
*/
#endif
